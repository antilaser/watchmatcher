# --- Same-origin (nginx on the VPS: / → UI, /api/v1 → API) ---
# Do not set NEXT_PUBLIC_API_BASE_URL; the browser uses /api/v1 on the same host.
#
# --- Cross-origin only (UI on another host than the API) ---
# NEXT_PUBLIC_API_BASE_URL=https://api-host.example/api/v1
